# R for travis: see documentation at https://docs.travis-ci.com/user/languages/r

language: R
cache: packages
sudo: required

apt_packages:
    - gdal-bin
    - libgdal-dev
    - libxml2-dev
    - libproj-dev
